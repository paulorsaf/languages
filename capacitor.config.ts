import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.paulo.languages',
  appName: 'Languages',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
